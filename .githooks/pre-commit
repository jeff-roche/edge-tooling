#!/bin/bash
set -euo pipefail
# Git pre-commit hook
#
# Install shared hooks from the repo root: make setup-githooks

REPO_ROOT="$(git rev-parse --show-toplevel)"
"$REPO_ROOT/scripts/lint-markdown.sh" --pre-commit
"$REPO_ROOT/scripts/sync-plugin-skills.sh" --verify

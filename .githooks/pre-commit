#!/bin/bash
# Git pre-commit hook — validates staged markdown files with markdownlint-cli2
#
# Install shared hooks from the repo root: make setup-githooks

REPO_ROOT="$(git rev-parse --show-toplevel)"
exec "$REPO_ROOT/scripts/lint-markdown.sh" --pre-commit
